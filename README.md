# Abhishek & Deshna — wedding invitation

Single-page invite: `index.html` (inline CSS + JS, no external resources), `logo-mark.svg` (vector AD monogram, traced from the original artwork) and `logo.jpg` (full applique artwork, used as the touch icon).

Every push to `main` deploys to GitHub Pages via `.github/workflows/pages.yml`.

Placeholders still to replace are marked `SAMPLE` / `SAMPLE-CONTACT` in `index.html`.
